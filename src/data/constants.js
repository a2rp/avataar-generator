export const STORAGE_KEYS = {
    theme: "avataar-generator-theme",
    favorites: "avataar-generator-favorites",
    settings: "avataar-generator-settings",
};

export const DEFAULT_SEED = "a2rp";

export const DEFAULT_CONTROLS = {
    scale: 100,
    rotate: 0,
    radius: 0,
    margin: 0,
    flip: false,

    backgroundTransparent: true,
    backgroundColor: "#ffffff",
};

export const MAX_FAVORITES = 40;

export const EXPORT_SIZE = 1024;

export const TOAST_DURATION = 2400;
